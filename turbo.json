{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  },
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["./apps/flex-visualize/dist/**"]
    }
  }
}
